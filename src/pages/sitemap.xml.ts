import type { APIRoute } from 'astro'; import { getCollection } from 'astro:content'; import { site } from '../lib/site';
export const GET: APIRoute = async () => { const [products, articles] = await Promise.all([getCollection('products'), getCollection('articles')]); const routes = ['','/products','/new','/engineering','/quality','/sourcing','/knowledge','/resources','/questionnaire','/contact', ...products.map(p=>`/products/${p.id}`), ...articles.map(a=>`/knowledge/${a.id}`)]; return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${routes.map(route=>`<url><loc>${site.url}${route}</loc></url>`).join('')}</urlset>`, { headers: { 'Content-Type':'application/xml' } }); };
